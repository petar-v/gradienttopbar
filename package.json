{
  "name": "gnome-gradienttopbar",
  "title": "Gradient Top Bar",
  "description": "Give the top bar a customizable gradient. Choose direction and colors, and optionally use a different style or keep the theme when windows are maximized.",
  "version": "26",
  "engines": {
    "gnome": ">=49"
  },
  "shell-version": [
    "49",
    "50",
    "51"
  ],
  "uuid": "gradienttopbar@pshow.org",
  "url": "https://github.com/petar-v/gradienttopbar",
  "license": "WTFPL",
  "author": "Petar Vasilev <petar@pshow.org>",
  "type": "module",
  "scripts": {
    "nested-wayland": "dbus-run-session -- gnome-shell --devkit --wayland",
    "pref-debug": "journalctl -f -o cat /usr/bin/gjs",
    "open-prefs": "gnome-extensions prefs gradienttopbar@pshow.org",
    "local-install": "yarn validate-schemas && yarn zip && gnome-extensions install gradienttopbar@pshow.org.shell-extension.zip --force && gnome-extensions enable gradienttopbar@pshow.org",
    "zip-extension": "gnome-extensions pack src --force",
    "validate-schemas": "glib-compile-schemas --strict --dry-run ./src/schemas/",
    "zip": "cd src && zip -FS -r ../gradienttopbar@pshow.org.shell-extension.zip .",
    "prettify": "prettier --write 'src/**/*.{css}' && yarn lint --fix",
    "lint": "eslint 'src/**/*.{js,json}'",
    "setup-python": "python3 -m venv .venv && .venv/bin/python -m pip install -r requirements-dev.txt",
    "shexli": ".venv/bin/shexli --format json \"$PWD/src\" | .venv/bin/python -c \"import json, sys; report = json.load(sys.stdin); print(json.dumps(report, indent=2)); raise SystemExit(report['summary']['finding_count'] != 0)\"",
    "verify": "yarn lint && yarn shexli",
    "precommit": "lint-staged",
    "upload": "gnome-extensions upload --accept-tos"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-prettier": "^5.5.6",
    "husky": "^9.1.7",
    "prettier": "^3.9.9"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn prettify && yarn lint"
    }
  },
  "lint-staged": {
    "src/**/*.{js,css,json}": [
      "yarn prettify",
      "yarn lint",
      "git add"
    ]
  },
  "packageManager": "yarn@4.0.1"
}
